#!/usr/bin/env bash
echo '
	Useful Commands
		- :normal, :normal!, :execute
		- mq and `q store and retrieve location
		- :normal! -> ignore previous mapping
	Vimscript supports
		- Scoped variables
		- Loop, list
		- Dictionary
	Group autocommand
		- Why? -> Prevent duplicate autocommands when sourcing .vimrc again
	Operator-Pending Mapping
		- Why? -> Use with operator which waits for another command likes d, y, c
	Conditions
		- ==# and ==? -> always case-sensitive and case-insensitive
	Notes
		- Sometimes <c-v> is used to escape <esc>
'
